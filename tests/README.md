# Backend Tests

This directory contains backend FastAPI tests for the Mergington High School Activities API.

## Running tests

Install dependencies and run `pytest` from the repo root:

```bash
pip install -r requirements.txt
pytest -q
```

## Test style

The tests use the Arrange-Act-Assert (AAA) pattern for clarity and maintainability.
